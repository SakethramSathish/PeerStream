"""Command-line front-end for the BitTorrent client.

The CLI drives the same application services the desktop UI will use, so it is
both the fastest way to debug protocol behaviour and the proof that the engine
works headlessly.
"""
